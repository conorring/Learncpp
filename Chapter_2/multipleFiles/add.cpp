#include "add.h" //it is generally good practice to include the header declaring the function
                 // in the file where the function is defined to catch discrepancies such as return or parameter mistakes

int add(int x, int y)
{
    return x + y;
}